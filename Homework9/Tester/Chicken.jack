class Chicken{
    field int location;
    field int score;

    constructor Chicken new(int x){
        let location = x;
        let score = 0;
        do draw();
        return this;
    }

    method void dispose(){
        do Memory.deAlloc(this);
        return;
    }

    method void draw(){
        do Graphics.drawChicken1(location);
        return;
    }

    method void jump(){
        var int key;
        let key = Keyboard.keyPressed();
        if (key = 32){
            let score = score + 1;
            do Graphics.chickenReset(location);
            do Graphics.chickenJump(4804);
            do Sys.wait(1000);
            do Graphics.chickenReset(4804);
            do Graphics.drawChicken1(location);   
        }
       
        return;
        
    }

    method int getScore(){
        return score;
    }

}
        

